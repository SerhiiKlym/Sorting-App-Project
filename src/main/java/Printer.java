import java.util.Arrays;

/**
 * Handles the formatting and printing of the sorted integers to the standard output.
 * It might convert the sorted list into a specific format for display.
 */
public class Printer {
    /**
     * Prints the sorted array of integers in a user-friendly format.
     *
     * @param sortedNumbers an array of sorted integers to be printed
     * @return
     */
    public String print(int[] sortedNumbers) {
        // Printing logic
        return Arrays.toString(sortedNumbers);
    }
}
